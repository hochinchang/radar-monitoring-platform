version: '3'
services:
  nginx:
    image: nginx:latest
    ports:
      - "3000:3000"
    volumes:
      - /data/b615/radar-monitoring-platform/nginx.conf:/etc/nginx/conf.d/default.conf
      - /data/b615/radar-monitoring-platform/frontend:/usr/share/nginx/html
    restart: unless-stopped

